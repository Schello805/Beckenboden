"use client";
import { useEffect,useState } from "react";
function decode(value:string){const padding="=".repeat((4-value.length%4)%4),base64=(value+padding).replace(/-/g,"+").replace(/_/g,"/"),raw=atob(base64);return Uint8Array.from([...raw].map(char=>char.charCodeAt(0)))}
export function PushPreference(){const [state,setState]=useState<{available:boolean;publicKey:string|null;enabled:boolean}|null>(null),[notice,setNotice]=useState("");useEffect(()=>{fetch("/api/push").then(r=>r.json()).then(setState)},[]);async function enable(){if(!state?.publicKey||!("serviceWorker" in navigator)||!("PushManager" in window)){setNotice("Push wird auf diesem Gerät nicht unterstützt.");return}const permission=await Notification.requestPermission();if(permission!=="granted"){setNotice("Benachrichtigungen wurden nicht erlaubt.");return}const registration=await navigator.serviceWorker.ready,subscription=await registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:decode(state.publicKey)}),response=await fetch("/api/push",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(subscription)});if(response.ok){setState({...state,enabled:true});setNotice("Push-Benachrichtigungen sind aktiv.")}}async function disable(){const registration=await navigator.serviceWorker.ready,subscription=await registration.pushManager.getSubscription();await subscription?.unsubscribe();await fetch("/api/push",{method:"DELETE"});if(state)setState({...state,enabled:false});setNotice("Push-Benachrichtigungen sind ausgeschaltet.")}if(!state?.available)return null;return <div className="push-preference"><b>Benachrichtigungen</b><p>Erhalte eine kurze Nachricht, wenn deine Teilnahme eingetragen wurde.</p><button className={state.enabled?"":"primary"} onClick={state.enabled?disable:enable}>{state.enabled?"Ausschalten":"Push aktivieren"}</button>{notice&&<small>{notice}</small>}</div>}
