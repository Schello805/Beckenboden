"use client";
import {FormEvent,useEffect,useRef,useState} from "react";
export function SupportForm(){const [notice,setNotice]=useState(""),[subject,setSubject]=useState("support"),section=useRef<HTMLElement>(null);useEffect(()=>{const open=()=>{setSubject("makeup");window.setTimeout(()=>section.current?.scrollIntoView({behavior:"smooth",block:"center"}),50)};window.addEventListener("open-makeup-request",open);return()=>window.removeEventListener("open-makeup-request",open)},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget,r=await fetch("/api/support",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form).entries()))}),x=await r.json();setNotice(r.ok?subject==="makeup"?"Deine Anfrage wurde an die Kursleitung gesendet. Sie meldet sich persönlich bei dir.":"Deine Nachricht wurde an Anja gesendet.":x.error);if(r.ok){form.reset();setSubject("support")}}return <section ref={section} className="support-card"><p className="eyebrow">Direkter Kontakt</p><h2>Support & Feedback</h2><p>Keine Notfall- oder medizinische Beratung. Für organisatorische Fragen, Feedback oder einen Nachholtermin.</p><form onSubmit={submit}><select name="subject" value={subject} onChange={event=>setSubject(event.target.value)}><option value="support">Supportanfrage</option><option value="feedback">Feedback</option><option value="makeup">Nachholtermin anfragen</option></select><textarea name="message" minLength={10} maxLength={5000} required placeholder={subject==="makeup"?"Welchen Termin möchtest du nachholen? Die Kursleitung meldet sich persönlich bei dir.":"Deine Nachricht an Anja …"}/><button className="primary">Nachricht senden</button></form>{notice&&<p role="status">{notice}</p>}</section>}
