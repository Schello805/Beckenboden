"use client";
import { FormEvent,useState } from "react";
export function SupportForm(){const [notice,setNotice]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget,r=await fetch("/api/support",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form).entries()))}),x=await r.json();setNotice(r.ok?"Deine Nachricht wurde an Anja gesendet.":x.error);if(r.ok)form.reset()}return <section className="support-card"><p className="eyebrow">Direkter Kontakt</p><h2>Support & Feedback</h2><p>Keine Notfall- oder medizinische Beratung. Für organisatorische Fragen, Feedback oder einen Nachholtermin.</p><form onSubmit={submit}><select name="subject"><option value="support">Supportanfrage</option><option value="feedback">Feedback</option><option value="makeup">Nachholtermin anfragen</option></select><textarea name="message" minLength={10} maxLength={5000} required placeholder="Deine Nachricht an Anja …"/><button className="primary">Nachricht senden</button></form>{notice&&<p role="status">{notice}</p>}</section>}
