"use client";
import { FormEvent,useEffect,useState } from "react";
export function AdminPush(){const [settings,setSettings]=useState<{configured?:boolean;publicKey?:string;subject?:string}>({}),[notice,setNotice]=useState("");useEffect(()=>{fetch("/api/admin/settings/push").then(r=>r.json()).then(setSettings)},[]);async function configure(e:FormEvent<HTMLFormElement>){e.preventDefault();const subject=new FormData(e.currentTarget).get("subject"),response=await fetch("/api/admin/settings/push",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({subject})}),result=await response.json();if(!response.ok){setNotice(result.error);return}setSettings({configured:true,publicKey:result.publicKey,subject:String(subject)});setNotice("Push-Schlüssel wurden erzeugt und verschlüsselt gespeichert.")}return <article className="panel security-panel"><small>BENACHRICHTIGUNGEN</small><h2>Web Push</h2><p>Web Push läuft direkt über Browser und Betriebssystem. Es ist kein Firebase-Konto nötig. Push-Nachrichten enthalten keine Gesundheits- oder Kontaktdaten.</p>{settings.configured?<div className="security-ok"><b>✓ Web Push ist eingerichtet</b><p>Kontakt: {settings.subject}</p></div>:<form className="admin-form" onSubmit={configure}><label>Technischer Kontakt <small>z. B. mailto:kontakt@example.de</small><input name="subject" required placeholder="mailto:kontakt@example.de"/></label><button className="primary">Push sicher einrichten</button></form>}{notice&&<p>{notice}</p>}</article>}
